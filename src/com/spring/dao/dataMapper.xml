<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
	namespace：必须与对应的接口全类名一致
	id:必须与对应接口的某个对应的方法名一致
	
 -->
<mapper namespace="com.spring.dao.DataMapper">	
	<select id="findAll" resultType="Data">
		select electricity electricity,voltage voltage,sensor_Num sensor_Num,machine_id machine_id,welder_id welder_id,code code,year year,month month,day day,hour hour,minute minute,second second,status status from tb_data;
	</select>
	
</mapper>